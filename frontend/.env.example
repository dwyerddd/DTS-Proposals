# Copy this file to .env and adjust if needed.
# Points the frontend at the backend API. The default matches uvicorn's
# default local address, so most of the time this doesn't need changing.
VITE_API_URL=http://127.0.0.1:8000
